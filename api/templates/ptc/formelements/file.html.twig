
<div class="input input--horizontal">
    {% if not app.user %}
    <aside class="hint">
        <h2 class="hint__title">{{ 'login required'|trans|capitalize}}</h2>
        {{ 'you need to login before you can upload files'|trans|capitalize}}
    </aside>
    {% else %}
    <div>
        <label class="input__label" for="{{ property.id }}">{{ property.title }}</label>
        {% if property.description is defined %}
        <span class="input__hint" id="hint_name">Bijvoorbeeld Frank Masmeijer</span>
        {% endif %}
    </div>
    <input type="file" id="{{ property.id }}">" aria-describedby="hint_name" class="input__control input__control--text input__control--l" name="request[properties][{{ property.name }}]">
    {% endif %}
</div>

